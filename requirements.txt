# ── Core framework ────────────────────────────────────────────────────────
pydantic>=2.7,<3
pydantic-settings>=2.3,<3

# ── Web server ────────────────────────────────────────────────────────────
fastapi>=0.111,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1          # async HTTP client used by FastAPI TestClient
python-multipart>=0.0.9 # form/file uploads for FastAPI

# ── Config & serialisation ────────────────────────────────────────────────
pyyaml>=6.0,<7
python-dotenv>=1.0,<2

# ── LLM backends ─────────────────────────────────────────────────────────
openai>=1.30,<2         # OpenAI Chat Completions (baseline_inference.py)
requests>=2.31,<3       # HuggingFace Inference Endpoint backend

# ── Data handling ─────────────────────────────────────────────────────────
numpy>=1.26,<3          # numerical ops (scoring, metrics)

# ── Testing ───────────────────────────────────────────────────────────────
pytest>=8.0,<9
pytest-asyncio>=0.23,<1
pytest-cov>=5.0,<6      # coverage reports

# ── Dev / linting (optional but recommended) ─────────────────────────────
ruff>=0.4,<1            # linter + formatter
mypy>=1.10,<2           # static type checker